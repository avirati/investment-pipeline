#!/usr/bin/env bash
# Thin wrapper so nobody needs to know pnpm exists to run this.
# It stays a wrapper, not a task runner (CLAUDE.md, ARCHITECTURE §7.1).
set -euo pipefail
cd "$(dirname "$0")"
exec pnpm exec tsx src/cli.ts "$@"
